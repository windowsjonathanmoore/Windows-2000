"SELECT Measures.members ON ROWS, [Product Category].members ON COLUMNS FROM [Bobs Video Store]"
"SELECT [Customer Age].members ON ROWS , Nest([Product Category].members,Measures.members) ON COLUMNS FROM [Bobs Video Store]"
"SELECT Measures.members ON ROWS, Descendants([1995],[Month]) ON COLUMNS FROM [Bobs Video Store]"
"SELECT Measures.members ON ROWS, order([Product Category].members,Movies.currentmember.name) ON COLUMNS FROM [Bobs Video Store]"
"SELECT Measures.members ON ROWS, order([Product Category].members, [Total Amount] ) ON COLUMNS FROM [Bobs Video Store]"
"SELECT Measures.members ON ROWS, order([Product Category].members, [Total Amount],desc ) ON COLUMNS FROM [Bobs Video Store]"
"SELECT Measures.members ON ROWS,  {[Action/Adventure], Thriller, Foreign, Western} ON COLUMNS FROM [Bobs Video Store]"
"SELECT Measures.members ON ROWS, hierarchize({[Action/Adventure], Thriller, Foreign, Western}) ON COLUMNS FROM [Bobs Video Store]"
"SELECT Measures.members ON ROWS,topcount([Product Category].members,5, [Total Amount]) ON COLUMNS FROM [Bobs Video Store]"
"SELECT Measures.members ON ROWS,bottomcount([Product Category].members,5,Quantity ) ON COLUMNS FROM [Bobs Video Store]"
"SELECT Measures.members ON ROWS,filter([Product Category].members,Quantity<20000 ) ON COLUMNS FROM [Bobs Video Store]"
"SELECT Measures.members ON ROWS,  extract({([Action/Adventure],time.[1995]), (Thriller,time.[1994]), (Foreign,time.[1994]), (Western,time.[1996])}, [time]) ON COLUMNS FROM [Bobs Video Store]"
"SELECT [Customer Age].members ON ROWS , extract(Nest([Product Category].members,Measures.members), [Movies]) ON COLUMNS FROM [Bobs Video Store]"
"SELECT Measures.members ON ROWS,  extract({([Action/Adventure],time.[1995]), (Thriller,time.[1994]), (Foreign,time.[1994]), (Western,time.[1996])}, [time]) ON COLUMNS FROM [Bobs Video Store]"
"SELECT Generate([Store Location].[Store City].Members, [Store Location].CurrentMember.children) ON COLUMNS,  Measures.Members ON ROWS FROM [Bobs Video Store]"
"SELECT Generate(Filter([Store Location].[Store Description].Members, [Measures].[Quantity]>0), {[Store Location].CurrentMember.Parent}) ON COLUMNS,[Measures].Members ON ROWS FROM [Bobs Video Store]"
"SELECT Filter([Store Location].[Store Description].Members, [Measures].[Quantity]>0) ON COLUMNS,[Measures].Members ON ROWS FROM [Bobs Video Store]"
"SELECT Generate([Store Location].[Store Description].Members, {[Store Location].CurrentMember.Parent}) ON COLUMNS,[Measures].Members ON ROWS FROM [Bobs Video Store] WHERE ([1995])"
