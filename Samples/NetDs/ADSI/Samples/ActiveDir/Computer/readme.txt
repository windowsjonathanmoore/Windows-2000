
--- This samples creates a machine account in Active Directory using ADSI LDAP provider----

You're required to supply a new machine account. Once the object is created you'll be able to 
join the machine to a domain. By default (via GPO, Group Policy Objects), in Windows 2000 domain, every authenticate users are able to 
join to a domain. 
