-- Active Directory Services Samples: Reset Password ------

Reset Windows NT Password. The user does not require to know the current password, but the 
user must have privilaget to reset one's password.
You can change your domain password or your local account password.

This sample uses WinNT provider exclusively.

